export interface Category {
  name: string,
  tasks: object[],// it will need to be changed into a interface type of each entry of an array
  notes: object[],// it will need to be changed into a interface type of each entry of an array
}
